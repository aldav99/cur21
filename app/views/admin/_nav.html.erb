<div class="nav nav-admin">
  <%= t('title_html') %>
  
  <div class="nav-admin user">
    <%= t('welcome_html') %>, <b class="nav-admin user-email"><%= current_user.email %></b> Guru | <%= link_to t('logout'), destroy_user_session_path, method: :delete %>
  </div>
</div>